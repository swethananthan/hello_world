#!/bin/bash

echo "Hello World!"


cat << EOM
- Adding ssh keys to github: https://docs.github.com/en/authentication/connecting-to-github-with-ssh/adding-a-new-ssh-key-to-your-github-account
- Linux Commands
  - Create directory: mkdir <directory name>
  - Enter inside directory: cd <directory>
  - Moved to previous directory: cd ..
  - Move to root directory: cd /
  - "/" means the top level (root) directory in linux
EOM
